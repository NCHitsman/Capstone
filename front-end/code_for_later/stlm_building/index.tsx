
// const [action, setAction] = useState('') // * USED TO CONTROL USER ACTION

// const array: number[] = [] // * USED TO MAKE ARRAY TO MAP OVER BELOW
// if (world) {
//     for (let i = 0; i <= world?.world_size; i++) {
//         array.push(i)
//     }
// }


{/* {array.map((a, i) => { // * THE BOTTOM PLANE USED FOR SETTLEMENT PLACING
                        return (
                            array.map((b, j) => {
                                return (
                                    <WorldBaseSquare key={b} action={action} setAction={setAction} i={i} j ={j} worldSize={world.world_size}/>
                                )
                            })
                        )
                    })} */}
